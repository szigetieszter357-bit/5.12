import { GeoPoint } from '../../models/Location';

// M5 will replace this temporary center point with the device location.
export const DEFAULT_MOCK_LOCATION: GeoPoint = {
  latitude: 31.2304,
  longitude: 121.4737
};
